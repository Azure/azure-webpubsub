# Exclusions for azd source upload (App Service code deploy)
# Add sensitive and dev-only files here. Similar semantics to .gitignore.

# Local environment files (prevent accidental secret deployment)
.env
*.env
.env.*

# Node modules (massive, rebuilt in build step if needed)
client/node_modules
node_modules

# Git metadata
**/.git

# Python virtual environments & caches
**/.venv
**/__pycache__
*.py[cod]
*.log

# Frontend intermediate artifacts (final assets copied into python_server/static by prepackage hook)
client/.vite
client/dist

# IMPORTANT: Do NOT exclude python_server/static – it contains the built client served by the backend.
# (Removed previous exclusion of python_server/static)

# Editor / platform cruft
.vscode
.idea
Thumbs.db
.DS_Store
