import { ClientPannelProps } from "../Playground";

// TODO: add a client section for supported subprotocols using client SDKs
export const SubprotocolClientSection = ({ onStatusChange, url }: ClientPannelProps) => <div>{url}</div>;
